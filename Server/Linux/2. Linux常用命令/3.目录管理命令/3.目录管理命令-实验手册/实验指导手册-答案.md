## 实验任务： 目录管理命令
- 专业:
- 学号:
- 班级:
- 姓名:
- 实验日期(xxxx年xx月xx日)：
- 实验成绩:

### 一、实验环境

- 提供Vmware Workstation 和 XX 版本 Linux 虚拟机

### 二、**实验目标**

- 学会使用目录管理命令相关命令

### 三、实验指导

#### 3.1、实验原理

##### pwd
- pwd代表的是“Print Working Directory”(打印工作目录)。pwd会打印出当前工作目录，或简单的来说就是当前用户所位于的目录。它会打印出以根目录(/)为起点的完整目录名(绝对路径)
- 这条命令是一条shell内建命令，并且在大多数shell中都可以使用。(bash、zsh、ksh)

```shell
/bin/pwd [OPTION]

#有关“/bin/pwd”和“pwd”之间的区别。“pwd”意味着使用shell内置的pwd。你的shell可能有不同版本的pwd

#当使用/bin/pwd时，我们调用的是二进制版本的命令。虽然二进制的版本有更多的选项，但是它们两者都能打印当前的目录。
```


##### mkdir
- mkdir命令用来创建目录。该命令创建由dirname命名的目录。如果在目录名的前面没有加任何路径名，则在当前目录下创建由dirname指定的目录；如果给出了一个已经存在的路径，将会在该目录下创建一个指定的目录。在创建目录时，应保证新建的目录与它所在目录下的文件没有重名

- 语法:
```shell
mkdir [option] directory

option：mkdir命令的选项

directory：mkdir命令创建的目录名也可以包含路径
```


##### rmdir
- rmdir命令用来删除空目录。当目录不再被使用时，或磁盘空间已到达使用限定值，就需要删除失去使用价值的目录
- 利用rmdir可以从一个目录中删除一个或多个空的子目录。该命令从一个目录中删除一个或多个子目录，删除目录时，必须具有对其父目录的写权限
- 语法:
```shell
rmdir [option] directory

option：rmdir命令的选项

directory：需要删除的空目录
```

##### cd
- 在Linux中，cd命令，是对新手和系统管理员来说，最重要最常用的命令。对管理无图形界面的服务器的管理员，cd是进入目录，检查日志，执行程序/应用软件/脚本和其余每个任务的唯一方法。
- 语法:
```shell
cd [option] directory

option：cd命令的选项

directory：需要进入的目录

dir的技巧:

cd - #回到上次所在目录

cd #回到主目录

cd   / #切换到根目录

cd   .. #切换到上级目录
```


##### ls
- ls命令是linux下最常用的命令。ls命令就是list的缩写，缺省下ls用来打印出当前目录的清单，如果ls指定其他目录，那么就会显示指定目录里的文件及文件夹清单。
- 通过ls 命令不仅可以查看linux文件夹包含的文件，而且可以查看文件权限(包括目录、文件夹、文件权限)，查看目录信息等等。
- 语法:
```shell

ls [option] file

option：ls命令的选项

file：ls命令的目标目录或文件

缺省下ls用来打印出当前目录的清单

```

##### tree

- 使用tree命令以树状图递归的形式显示各级目录，可以方便地看到目录结构。
- 语法:
```shell
tree [option] directory

option：tree命令的选项

directory：所需解析的目录
```

##### tar

- tar用于将文件打包或解包，扩展名一般为“.tar”，指定特定参数可以调用gzip或者bzip2制作压缩包或解开压缩包，扩展名为“.tar.gz”或者“.tar.bz2”。

- 语法:
```shell
tar [option] tarfile tarsourcefile

option：tar命令的选项

tarfile：打包之后的文件名比如file.tar

tarsourcefile:需要打包的源文件
```

##### zip/unzip

- zip是Linux系统下广泛使用的压缩程序，文件压缩后扩展名为“.zip”。

- 语法:
```shell
zip [option] zipsourcefile zipdestfile

option：zip命令的选项

zipsourcefile：压缩后的文件名

zipdestfile: 打包的目录路径
```


##### gzip

- 文件经过gzip压缩后，文件会多出一个 .gz后缀。gzip命令对文本文件有60%～70%的压缩率

- 语法:
```shell
gzip [option] file

option：gzip命令的选项

file: 要压缩的文件
```

#### 3.2、实验步骤
- **以下实验中所有的XX为学号**

##### pwd

- 查看当前所在目录,将所用到的指令和指令执行结果截图

     `pwd`

- 在/opt目录下创建一个名为yumXX的目录并指向/etc/yum.repos.d,并利用pwd查看执行结果,将所用到的指令和指令执行结果截图

     `ln -s /etc/yum.repos.d/ yumXX`
     `pwd -L`
##### mkdir

- 在当前目录下新建目录abcXX,将所用到的指令和指令执行结果截图

     `mkdir abcXX`

- 查看mkdir命令的版本信息,将所用到的指令和指令执行结果截图

     `mkdir -version`

##### rmdir

- 删除/opt目下的空abcXX目录,将所用到的指令和指令执行结果截图

     `rmdir /opt/abcXX`

- 在/opt目录下创建abcXX目录，并在abcXX目录下创建testXX目录，删除/opt目录下abcXX目录和其子目录,将所用到的指令和指令执行结果截图

     `rmdir -p /opt/abcXX

- 在/opt目录下创建abcXX目录，并在abcXX目录下创建testXX目录，删除/opt目录下abcXX目录和其子目录,并显示详细信息,将所用到的指令和指令执行结果截图

     `rmdir -pv /opt/abcXX`


##### cd
- 快速回到用户主目录,将所用到的指令和指令执行结果截图

     `cd`

- 快速回到上次所在目录,将所用到的指令和指令执行结果截图

     ```
     cd /etc
     
     cd
     
     cd -       //回到/etc
     ```
- 快速回到上一级目录,将所用到的指令和指令执行结果截图
```
cd /etc

cd ..         //(回到/目录)
```
- 切换到/usr/share目录,将所用到的指令和指令执行结果截图
```
cd /usr/share
```
- 把一个命令的参数作为cd的参数,将所用到的指令和指令执行结果截图
```shell
mkdir /opt/test

cd !$   //相当于cd /opt/test
```
##### ls

- 以易读的方式列出当前目录下文件和目录的详细信息,将所用到的指令和指令执行结果截图

     `ll -h`

- 显示当前目录下的文件和目录的inode 号,将所用到的指令和指令执行结果截图

     `ls -i`

- 以KB为单位显示当前目录下的文件大小,将所用到的指令和指令执行结果截图

     `ls -k`
 - 查看当前目录下的所有文件和目录,将所用到的指令和指令执行结果截图
 
   `ls -a`
   
 - 递归显示,将所用到的指令和指令执行结果截图
 
   `ls -R`

##### tree

- 使用tree命令以树状图递归的形式显示各级目录，可以方便地看到目录结构。

- 显示当前的目录下个各级目录,将所用到的指令和指令执行结果截图

	 `tree       //在每个文件或目录之前，显示完整的相对路径名称`

##### tar

- 将/etc打包到/tmp/etc.tar（只打包不压缩）,将所用到的指令和指令执行结果截图

     `tar -cvf /tmp/etc.tar /etc`

- 将/etc打包到/tmp/etc.tar.gz(打包并使用gzip压缩文件),将所用到的指令和指令执行结果截图

     `tar -zcvf /tmp/etc.tar.gz /etc`

- 解压/tmp/etc.tar.gz到当前目录,将所用到的指令和指令执行结果截图

     `tar -zxvf /tmp/etc.tar`

##### zip/unzip

- 将当前文件夹压缩成file_backup.zip,将所用到的指令和指令执行结果截图

     `zip -r file_backup.zip .`

- 将file_backup.zip文件解压到/data/backup文件夹中,将所用到的指令和指令执行结果截图

    `unzip file_backup.zip -d /data/backup`

##### gzip

- 将temp压缩成temp.gz,将所用到的指令和指令执行结果截图

     `gzip -c temp > temp.gz`

- 将temp.gz文件解压缩,将所用到的指令和指令执行结果截图

    `gzip -d temp.gz`

### 四、实验过程

### 五、实验总结及自我评价

### 六、任务拓展