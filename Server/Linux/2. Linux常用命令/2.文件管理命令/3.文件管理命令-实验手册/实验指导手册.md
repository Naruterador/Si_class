## 实验任务： 文件管理命令
- 专业:
- 学号:
- 班级:
- 姓名:
- 实验日期(xxxx年xx月xx日)：
- 实验成绩:

### 一、实验环境

- 提供Vmware Workstation 和 XX 版本 Linux 虚拟机

### 二、**实验目标**

- 学会文件管理的相关命令

### 三、实验指导

#### 3.1、实验原理

##### cp
- cp命令用来将一个或多个源文件或者目录复制到指定的目的文件或目录。它可以将单个源文件复制成一个指定文件名的具体的文件或一个已经存在的目录下。
- cp命令还支持同时复制多个文件，当一次复制多个文件时，目标文件参数必须是一个已经存在的目录，否则将出现错误。
- 语法:

```shell
cp  [OPTION]  SOURCE  DEST

cp  [OPTION]  SOURCE  DIRECTORY

OPTION：cp命令的选项

SOURCE：源文件

DEST：目标文件

DIRECTORY：目录
```


##### mv
- mv命令用来对文件或者目录重新命名，或者将文件从一个目录移动到另一个目录
- mv命令可以用来将源文件移至一个目标文件中，或将一组文件移至一个目标目录中源文件被移至目标文件有两种不同的结果
- mv命令用来对文件或者目录重新命名，或者将文件从一个目录移动到另一个目录
- mv命令可以用来将源文件移至一个目标文件中，或将一组文件移至一个目标目录中。源文件被移至目标文件有两种不同的结果
- 如果目标文件是到某一目录文件路径，源文件会被移到此目录下，且文件名不变
- 如果目标文件不是目录文件，则源文件名（只能有一个）会变为此目标文件名，并覆盖已存在的同名文件。如果源文件和目标文件在同一个目录下，mv的作用就是改文件名。当目标文件是目录文件时，源文件或目录参考可以有多个，则所有的源文件都会被移至目标文件中。所有移到改目录下的文件都将保留以前的文件名。

- 语法:
```shell
mv  [OPTION]  SOURCE  DEST

mv  [OPTION]  SOURCE  DIRECTORY

OPTION：mv命令的选项

SOURCE：源文件

DEST：目标文件

DIRECTORY：目录
```


##### touch
- touch命令的功能有2个: 
    - 用于把已存在的文件时间标签更新为系统当前时间，他们的数据将原封不动地保留下来
    - 用于创建新的空文件
- 语法:
```shell
touch  [OPTION]  file

OPTION：touch命令的选项

file：指定要设置时间属性的文件，或需要新建的文件名
```

##### rm
- rm命令可以删除一个目录中的一个或多个文件或目录，也可以将某个目录及其下属的所有文件及其子目录均删除掉。对于链接文件，只是删除整个链接文件，而原文件保持不变。
- 语法:
```shell
rm  [OPTION]  file

OPTION：mv命令的选项

file：需要删除的文件
```


##### df
- df命令用于检查文件系统的磁盘空间占用情况。可以利用该命令来获取硬盘被占用了多少空间，目前还剩下多少空间等信息.
- 语法:
- 
```shell

df [OPTION]  file

OPTION：df命令的选项

file：df命令操作对象

```

##### find

- find命令用于在目录结构中搜索文件或目录，并执行指定操作。linux下find命令提供了相当多的查找条件，功能很强大
- 由于find具有强大的功能，所以它的选项也很多，其中大部分选项都值得我们花时间来了解。即使系统中含有网络文件系统(NFS),find命令在改文件系统中同样有效，只要具有相应的权限。在运行一个非常消耗资源的find命令时，很多人都倾向于把它放在后台执行，因为遍历一个大的文件系统可能会花费很长的时间

- 语法:
```shell
find  pathname -options [-print -exec -ok ...]

pathname：find命令所查找的目录路径

-options：find命令的选项

-print：将结果输出到屏幕

-exec：对匹配文件执行该参数所给出的shell命令。相应命令的形式为'command' { }\;

-ok：和-exec的作用相同，只不过在执行每一个命令之前，都会给出提示，让我们来确定是否执行
```



##### du

- du(disk usage)命令可以计算文件或目录所占的磁盘空间。没有指定任何选项时，它会测量当前工作目录与其所有子目录，分别显示各个目录所占的块数，最后才显示工作目录所占总块数

- 语法:
```shell
du  [OPTION]  file

OPTION：du命令的选项

file：du命令操作对象
```


##### cat

- cat命令的用途是连接文件或标准输入并打印。这个命令常用来显示文件内容，或者将几个文件连接起来显示，或者从标准输入读取内容并显示，它常与重定向符号配合使用

- 语法:
```shell
cat  [OPTION]  file语法:

OPTION：cat命令的选项

file：cat命令操作对象

#如果没有指定文件，或者文件为”-”，则从标准输入读取
```


##### Head

- head命令用于显示文件开头的内容。默认情况下，head命令显示文件的头10行内容。

- 语法:
```shell
head  [OPTION]  file

OPTION：head命令的选项

file：head命令操作对象
```


##### less

- less 工具也是对文件或其它输出进行分页显示的工具，less是linux正统查看文件内容的工具，功能极其强大。less 的用法比起 more 更加的有弹性。
- 在 more 的时候，我们并没有办法向前面翻，只能往后面看，但less可以使用 [pageup] [pagedown] 等按键来往前往后翻看文件，更容易用来查看一个文件的内容！此外，在 less 里头可以拥有更多的搜索功能，不止可以向下搜，也可以向上搜。

- 语法:
```shell
less  [OPTION]  file

OPTION：less命令的选项

file：less命令操作对象
```


##### more

- more命令，功能类似 cat ，more会以一页一页的显示方便使用者逐页阅读，而最基本的指令就是按空白键（space）就往下一页显示，按 b 键就会往回（back）一页显示，而且还有搜寻字串的功能。

- 语法:
```shell
more [-dlfpcsu ] [-num ] [+/ pattern] [+ linenum] [filename ... ]

-num:一次显示的行数

+/pattern:在每个档案显示前搜寻该字串（pattern），然后从该字串前两行之后开始显示

+linenum:从第几行开始显示

filename:文件名，可以为多个文件
```

##### tail

- tail命令用于输出文件中的尾部内容。tail命令默认在屏幕上显示指定文件的末尾10行。如果给定的文件不止一个，则在显示的每个文件前面加一个文件名标题。如果没有指定文件或者文件名为“-”,则读取标准输入

- 语法:
```shell
tail  [OPTION]  file

OPTION：tail命令的选项

file：指定显示尾部内容的文件
```


##### grep

- grep（global search regular expression(RE) and print out the line，全面搜索正则表达式并把行打印出来）是一种强大的文本搜索工具，它能使用正则表达式搜索文本，并把匹配的行打印出来
- 语法:
```shell
grep  [OPTION]  pattern file

OPTION：grep命令的选项

pattern: 匹配模式

file：指定进行匹配的文件
```


#### 3.2、实验步骤
- **以下实验中所有的XX为学号**

##### touch

- 新建一个空白文档file1,将所用到的指令和指令执行结果截图

- 将file1的访问时间修改为当前系统时间,将所用到的指令和指令执行结果截图

- 如果当前目录下有名为file1的文件修改他的时间戳，如果没有不创建,将所用到的指令和指令执行结果截图
##### cp

- 将当前目录下的abcXX复制到目录/opt/下，并改名为filedocXX,将所用到的指令和指令执行结果截图

- 将/etc目录下的所有文件及其子目录复制到/tmp/shXX目录中，将所用到的指令和指令执行结果截图

- 将当前目录新建文件textXX_1.txt和textXX_2.txt并复制到/opt目录下,将所用到的指令和指令执行结果截图

##### mv

- 将在当前目录下新建文件 abc 并重命名为 cba,将所用到的指令和指令执行结果截图

- 在当前目录下创建doc1文件并移动到/opt目录下重命名为doc2,将所用到的指令和指令执行结果截图

- 将当前目录下的doc1、doc2移动到/usr目录下，如果/usr目录下有同名文件则备份,将所用到的指令和指令执行结果截图


##### rm

- 强制删除/opt目录下的所有文件和目录和子目录,将所用到的指令和指令执行结果截图

- 使用交互式凡是删除/opt/doc2目录及其目录下的所有文件和目录,将所用到的指令和指令执行结果截图


##### df

- 列出各文件系统的i节点使用情况,将所用到的指令和指令执行结果截图

- 列出文件系统的类型,将所用到的指令和指令执行结果截图

- 以k为单位显示磁盘的使用情况,将所用到的指令和指令执行结果截图

##### find

- 在/etc目录下查找大于2M的文件和目录,将所用到的指令和指令执行结果截图

- 在/etc目录查找以na开头的文件,将所用到的指令和指令执行结果截图

- 在当前目录下查找用户为apache的文件和目录,将所用到的指令和指令执行结果截图

- 在当前目录下查找所属组为apache的文件和目录并显示详细信息,将所用到的指令和指令执行结果截图

- 在/root目录下查找10分钟之内被修改过内容的文件,将所用到的指令和指令执行结果截图

- 在/root目下查找10分钟以前属性被修改过的文件,将所用到的指令和指令执行结果截图

- 在/etc目录下查找10分钟之内被访问过的文件,将所用到的指令和指令执行结果截图

- 在/opt目下删除不是以.apt结尾的文件,将所用到的指令和指令执行结果截图

- 在/tmp目录下查找i节点为3009的文件,将所用到的指令和指令执行结果截图


##### du

- 显示多个文件所占用的空间,将所用到的指令和指令执行结果截图

- 显示多个文件所占用的空间和所占用的空间总和,将所用到的指令和指令执行结果截图

- 显示一个目录及其子目录的磁盘使用情况,将所用到的指令和指令执行结果截图


##### cat

- 将/etc/passwd文件中的内容打印到屏幕并显示行号,将所用到的指令和指令执行结果截图


- 将/etc/profile文件中的内容打印到屏幕并显示行号（除空行外）,将所用到的指令和指令执行结果截图

- 将file1、file2中的内容输出到file3中,将所用到的指令和指令执行结果截图

##### head

- 将/etc/passwd文件中的前5行内容输出到屏幕上,将所用到的指令和指令执行结果截图

- 将/etc/passwd文件的前10行内容输出屏幕上，并且显示文件名,将所用到的指令和指令执行结果截图

##### less

- 分页输出进程到屏幕上,将所用到的指令和指令执行结果截图

- 分页输出历史命令到屏幕上,将所用到的指令和指令执行结果截图

- 分页输出/etc/passwd的内容到屏幕上,将所用到的指令和指令执行结果截图

##### more

- 显示/etc/passwd中从第三行起的内容,将所用到的指令和指令执行结果截图

- 显示/etc/passwd内容每屏显示5行,将所用到的指令和指令执行结果截图
 
- 显示doc.txt内容并把连续的多个空行显示为一行,将所用到的指令和指令执行结果截图

- 显示文件docfile的内容,将所用到的指令和指令执行结果截图


##### tail

- 显示/etc/passwd中后五行的内容,将所用到的指令和指令执行结果截图

- 显示/etc/passwd中后八行的内容,将所用到的指令和指令执行结果截图

##### grep

- 将/etc/passwd文件中以root开头的行打印到屏幕,将所用到的指令和指令执行结果截图

- 统计系统中有多少用户不能登录系统,将所用到的指令和指令执行结果截图

- 将/etc/profile文件中不包含then的行打印到屏幕，并显示行号,将所用到的指令和指令执行结果截图


### 四、实验过程

### 五、实验总结及自我评价

### 六、任务拓展